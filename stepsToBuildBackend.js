[
    `Initial commit: 
    1. Install node js
    2. Run "npm init -y" to initialize the package.json file that tracks dependencies
    3. Run "git init" to initialize the repository to be tracked by Git`
]